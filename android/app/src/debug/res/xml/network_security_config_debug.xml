<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug-only: allows plain HTTP to the emulator's host-loopback alias
         and the local dev machine, since the local FastAPI server has no
         TLS. Never merged into release builds (see AndroidManifest.xml in
         this same debug source set). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
